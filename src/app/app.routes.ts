// import { RouterModule, Routes } from '@angular/router';
// import { Component } from '@angular/core';

// const app-routes: Routes = [
//     { path: 'home', component: Component},
//     { path: '**', pathMach: 'full', redirecTo: 'home'}
// ];

// export const app_routing = RouterModule.forRoot(app-routes);
